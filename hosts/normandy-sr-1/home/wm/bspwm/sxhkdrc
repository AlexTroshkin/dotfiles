# Reload configuration
super + shift + Escape
	pkill -USR1 -x sxhkd

# Quit or restart bspwm
super + shift + {q,r}
	bspc {quit,wm -r}

# Close window
super + c
	bspc node -c

# Terminal
super + t
	alacritty

# Browser
super + b
	chromium
